import styles from "./App.module.css";

const App = () => {
  return (
    <div className={styles.app}>
      <main>
        <h1>Basic React + Webpack setup</h1>
        <p>This is a basic React + Webpack + Babel + Jest setup.</p>
      </main>
    </div>
  );
};

export default App;
